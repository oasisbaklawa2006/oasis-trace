# APPVERSE THREAD / AGENT ROUTING GUARDRAIL — TRACE

Canonical programme authority: `oasisbaklawa2006/Oasis-Baklawa-Central` → `APPVERSE_MISSION_CONTROL.md` and `appverse-control/state.json`.

## Repository authority

This repository owns the Trace application/client domain and Trace-specific contracts. Shared database/runtime authority and Appverse Supabase migration ownership remain in `oasis-supabase-core`; Central operational UI belongs in `Oasis-Baklawa-Central`.

## Fail-closed routing

If a pasted instruction, response, or requested change belongs to Core shared authority/migrations, Central operations, AI Studio, Buyer App, or another workstream outside the current Trace ASM mission:

`ROUTING REJECTED — instruction does not belong to this thread.`

Identify the likely ASM route when evidence permits, state `No code, PR, migration, deployment, or scope expansion performed.`, and stop. Do not silently absorb foreign scope.

If an upstream dependency is missing, report `BLOCKED`; never fabricate shared authority or bypass DPL/PI-carton, scan, custody, identity, or Core governance boundaries. `PR MERGED != STAGE CLEARED`; only Mission Control gates establish programme clearance.

Preserve or infer the current `ASM-ID`, `THREAD-ID`, `REPOSITORY`, `MISSION`, `DEPENDENCIES`, and `STOP CONDITION`. Return to Mission Control on gate completion, blocker, cross-repo requirement, merge milestone, or physical scanner/printer/device boundary.
